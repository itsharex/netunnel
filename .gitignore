# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Server & Agent binaries (built locally)
src/netunnel-server/server-run.exe
src/netunnel-server/netunnel-server-dev.exe
src/netunnel-agent/agent-run.exe
src/netunnel-agent/agent.exe

# Log files
*.log
*.err.log
*.out.log

# Build outputs
dist/
build/
*.egg-info/

# Node modules (for old netunnel-desktop prototype if it has pnpm)
src/netunnel-desktop/node_modules/

# Certificates (local dev only)
tools/*.pem
tools/*.pfx

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Tauri
src/netunnel-desktop-tauri/src-tauri/target/
src/netunnel-desktop-tauri/node_modules/

# Old prototype (废弃)
src/netunnel-desktop/

# Env files with secrets
.env
.env.*
!.env.example
!src/netunnel-desktop-tauri/.env.development
!src/netunnel-desktop-tauri/.env.production

# AI conversation logs (personal)
messages/

# Deploy artifacts
.deploy-artifacts/
deploy/

# Workspace root node_modules
node_modules/

# Agent build cache
src/netunnel-agent/.codex-cache/

# Server local config (secrets)
src/netunnel-server/config.local.yaml
src/netunnel-server/config.production.yaml

# NPC related
src/nps/
